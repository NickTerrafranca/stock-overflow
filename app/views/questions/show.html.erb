<p>
  <strong>Title</strong><br>
  <%= @question.title %><br>

  <strong>Description</strong><br>
  <%= @question.description %><br>

  <% unless @question.answers.empty? %>

    <% @question.answers.order('updated_at DESC').each do |q| %>
    <br><strong>Answer</strong><br>
      <%= q.answer_body %>
    <%end%>
  <%end%>
</p>

<h2>Answer this question</h2>
<%= form_for [@question, @answer] do |f| %>
  <%= f.label :answer_body %><br>
  <%= f.text_area :answer_body %><br>
  <%= f.submit 'Submit' %>
<%end%>

<br><%= link_to "Back to all Questions", questions_path %>
<br><%= link_to "Edit Questions", edit_question_path %>
<%= notice %>
<br><%= link_to "Delete this question", question_path, method: :delete %>
